{
  "name": "vozza",
  "version": "0.1.0",
  "private": true,
  "description": "Vozza — ditado por voz com IA, feito para o Brasil",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer && cp src/renderer/*.html dist/renderer/",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^33.0.0",
    "typescript": "^5.6.0"
  },
  "allowScripts": {
    "electron@33.4.11": true
  }
}
